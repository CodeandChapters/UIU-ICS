/*
Program that will define a constant using “DEFINE” and print the value.
*/
#include <stdio.h>
#define HEIGHT 200 // syntax:#define MACRO_NAME value, got it from geeksforgeeks.
int main()
{

    printf("The value of HEIGHT:%d", HEIGHT);

    return 0;
}